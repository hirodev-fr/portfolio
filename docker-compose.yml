include:
  - db-compose.yml
services:
  emdash:
    build: .
    ports:
      - '4321:4321'
    volumes:
      - emdash-data:/app/data
    restart: unless-stopped
    depends_on:
      - libsql

volumes:
  emdash-data:
